// Reglas de voz canónicas del coach IA.
//
// PLACEHOLDER (Lote Coach-A): este archivo existe como slot para la regla de
// voz unificada que se aplicará a TODOS los prompts user-facing en el Lote
// Coach-B. Hoy cada prompt define su tono ad-hoc, lo que produce drift
// (algunos en 2da persona, otros en 3ra con nombre del usuario).
//
// Cuando se decida la voz canónica (Lote B), exportar acá la constante
// COACH_VOICE_RULES y referenciarla desde los prompts user-facing.

export const COACH_VOICE_RULES_PLACEHOLDER = '';
